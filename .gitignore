build/
*.click
.clickable/
__pycache__/
*.pyc
.DS_Store

# Never commit credentials, signing material, or exported user data.
.env
.env.*
*.env
secrets.json
credentials.json
auth.json
*.key
*.pem
*_ed25519*
*.minisig
*.mind

# The committed EXPECTED public key for this app's release envelope (public
# half only; the private key lives outside every working tree).
!.release-keys/*.pub.pem
